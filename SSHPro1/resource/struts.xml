<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE struts PUBLIC "-//Apache Software Foundation//DTD Struts Configuration 2.1//EN" "http://struts.apache.org/dtds/struts-2.1.dtd">
<struts>
 <constant name="struts.action.extension" value="action"/> <!-- 指定请求后缀 -->
	<constant name="struts.devMode" value="true" /> <!-- 指定开发模式,方便调试 -->
	<constant name="struts.il8n.encoding" value="UTF-8"/> <!-- 指定编码 -->
	
	<constant name="struts.convention.package.locators" value="action" /><!-- 确定搜索包的路径。只要是结尾为action的包都要搜索。 -->
	
	
	
	<!-- 一般只需修改这一个位置 -->
	<constant name="struts.convention.package.locators.basePackage"
	value="com.zq.www.mis.action" /> <!-- 用于约定Action 类的根包 -->
		
		
	<constant name="struts.convention.action.name.separator" value="-" /><!-- 指定actionURL两个单词间分隔符 -->
	
	<!--
	
	零配置：约定大于配置，不需要去写xml映射规则
	 
	 约定规则：
	 
	相互映射：从路径找类，  从类找jsp/action(指定 result )
	
	OrderAction名：  （真正的后台处理类）
	
	访问规则：
	
	order.action
	
	order!p1.action   （访问路径）
	
	
	OrderListAction名：
	
	访问规则：order-list!p1.action
	 -->
 
	<constant name="struts.convention.default.parent.package"
		value="struts-default"/><!--  设置默认的父包 -->
   

</struts>    
